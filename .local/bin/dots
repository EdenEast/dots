#!/usr/bin/env bash

function main()
{
    [[ $# -eq 0 ]] && set -- status -s .

    # command_list=($(git help -a | grep "^  [a-z]" | tr ' ' '\n' | grep -v "^$"))
    # alias_list=($(git config --get-regexp alias | awk '{ print $1 }' | cut -c 7-))
    # for i in "${command_list[@]}" "${alias_list[@]}"
    # do
    #     echo $i
    # done

    git --git-dir=$HOME/.dots --work-tree=$HOME "$@"
}

main "$@"
